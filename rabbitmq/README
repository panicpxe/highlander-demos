# RabbitMQ Highlander Demo

MVP RabbitMQ environment for use with shipping log entries to ELK.

If you do not have an existing app to use the service, please run the `rabbitmq-demo-app` script to run a demo Java app. Requires maven, which will install if not present.
